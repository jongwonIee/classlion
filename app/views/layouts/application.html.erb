<!DOCTYPE html>
<html>
  <head>
    <title>Classlion</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>

    <!--for mobile resize-->
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <!-- Latest compiled and minified CSS -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css" integrity="sha384-BVYiiSIFeK1dGmJRAkycuHAHRg32OmUcww7on3RYdg4Va+PmSTsz/K68vbdEjh4u" crossorigin="anonymous">

    <!-- Optional theme -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap-theme.min.css" integrity="sha384-rHyoN1iRsVXV4nD0JutlnGaslCJuC7uwjduW9SVrLvRYooPp2bWYgmgJQIXwl/Sp" crossorigin="anonymous">

    <!-- Latest compiled and minified JavaScript -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js" integrity="sha384-Tc5IQib027qvyjSMfHjOMaLkfuWVxZxUPnCJA7l2mCWNIpG9mGCD8wGNIcPD7Txa" crossorigin="anonymous"></script>

    <link href="https://cdn.rawgit.com/YJSoft/Webfonts/0.1/BM_JUA.css" rel="stylesheet" type="text/css" />

    <style>
      .rolebutton {
        display: inline-block;
      }
    </style>
  </head>

  <body>
    <% flash.each do |message_type, message| %>
        <div class="alert alert-<%= message_type %>"><%= message %></div>
    <% end %>

    <header>
      <!--로그인 x-->
      <a href="/evaluations/index">CLASSLION</a> <input placeholder="강의명, 교수명, 전공명으로 검색해보세요!" type="text" name="courses" size="80">
      <% if logged_in? %>
          <div class="rolebutton">
            강평열람
            <% unless current_user.has_role? 'evaluator' %>
              <%= button_to '오픈하기', roles_evaluator_path, method: :post, :onClick => 'alert("강평열람 권한이 부여되었습니다")' %>
            <% else %>
              강의평가를 열람해보세요!
            <% end %>
          </div>
          <div class="rolebutton">
            위키열람
            <% unless current_user.has_role? 'wikier' %>
              <%= button_to '오픈하기', roles_wikier_path, method: :post, :onClick => 'alert("위키열람 권한이 부여되었습니다")'  %>
            <% else %>
                위키를 열람해보세요!
            <% end %>
          </div>
          <%= current_user.nickname %>
          <a href="/mypages/index">마이페이지</a>
          <%= link_to "로그아웃", logout_path, method: :delete %>
      <% else %>
          <%= link_to "로그인", login_path %>
      <% end %>
    </header>

    <%= yield %>

    <footer>
      <a href="#">ClassLion</a>
      <%= button_to '권한삭제', roles_remove_path, method: :post %>
    </footer>

  </body>
</html>