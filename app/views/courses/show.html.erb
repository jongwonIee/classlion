<h1>강의정보</h1>

      <p>교수님이름<%= @course.professor.name %></p>
      <p>강의명<%= @course.lecture.name %></p>
      <p>학교명<%= @course.university.local_name %></p>
      <p>강평수<%= @course.evaluation_count %></p>
      <p>전체평가평점</p>


<h1>이 강의의 강평리스트</h1>
<table>
<% @evaluations.each do |e| %>
    아이디:<%= e.user.nickname %><br>
    점수:<%= e.point_overall %><br>
    강평:<%= e.body.html_safe %><br>
    댓글:<br>
    <% e.comments.each do |c| %>
      아이디:<%= c.user.nickname %><br>
      내용:<%= c.body %><br>
    <% end %>
    <br>
      <%= form_for Comment.new do |f| %>
          <%= f.hidden_field :user_id, value: current_user.id %>

          <%= f.hidden_field :evaluation_id, value: e.id %>

          <%= f.text_field :body, autofocus: true, placeholder: "댓글"%><br/>

          <%= f.submit "작성하기" %>
      <% end %>
    <hr>
  <% end %>
</table>
